Interesting methods used for every day
----------------------------------------------------
Day1 : isdigit() - join
Day2 : RegexpTokenizer(r"\w+")
Day10 : padding
Day16 : Recursive function - from collections import Counter
Day17 : djikstra algorithm - apply - applymap - padding
Day18 : df.applymap(lambda x : ...
Day20 : all(element == 'a' for element in ma_liste) - dict(zip(dict_dest[module], ['l']*len(dict_dest[module])))
Day21 : index, colonne = (df.isin(['S'])).values.nonzero() - {tuple([4,5]) ,tuple([5,4])} 